{
  "name": "delta-pitwall",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "test": "vitest run --environment jsdom",
    "watch": "node build.mjs --watch"
  },
  "dependencies": {
    "three": "^0.160.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "jsdom": "^29.1.1",
    "vitest": "^4.1.5"
  }
}
